import React from 'react';
import './Book.css'
import '../../bootstrap.min.css'


function Book({title}) {
    return( 
        <div className="answer">
            <h4>{title}</h4>
        </div>
    )}

export default Book;